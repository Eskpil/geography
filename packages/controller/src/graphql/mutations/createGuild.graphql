mutation CreateGuild($options: GuildInput!) {
    createGuild(options: $options) {
        guild {
            id
            name
            image
            ownerId
            prefix
            session
        }
        errors {
            path
            message
        }
    }
}
